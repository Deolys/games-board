import styled from "@emotion/styled";

export const LogoLink = styled.a`
  display: flex;
  flex-direction: row;
  align-items: center;
  cursor: pointer;
  min-width: 180px;

  img {
    max-height: 60px;
  }
`;
